#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - the string length
 * @s: string length to print
 *
 * Return: return length of s
 */
int _strlen(char *s)
{
	int i;

	i = strlen(s);

	printf("%d, i");

	return (i);
}
